import { redirect } from 'next/navigation';
import { createClient } from '@/lib/supabase/server';
import { I18nText } from '@/components/LocaleShell';
export default async function InterestsPage(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/login');const {data:members}=await s.from('organization_members').select('organization_id,organizations(display_name,type)').eq('user_id',user.id);const ids=(members??[]).map((m:any)=>m.organization_id);const {data:items}=ids.length?await s.from('investor_interests').select('id,status,created_at,updated_at,opportunities(id,slug,owner_organization_id,investment_thesis,assets(title,city,country_code))').in('investor_organization_id',ids).order('updated_at',{ascending:false}):{data:[] as any[]};const {data:ownedOpps}=ids.length?await s.from('opportunities').select('id').in('owner_organization_id',ids):{data:[] as any[]};const ownedOppIds=(ownedOpps??[]).map((o:any)=>o.id);const {data:sellerItems}=ownedOppIds.length?await s.from('investor_interests').select('id,status,created_at,updated_at,investor_organization_id,opportunities(id,slug,owner_organization_id,investment_thesis,assets(title,city,country_code))').in('opportunity_id',ownedOppIds).order('updated_at',{ascending:false}):{data:[] as any[]};return <main className="app-shell"><header className="app-header"><a className="brand" href="/">ASSETVEYRA</a><nav><a href="/workspace"><I18nText id="Workspace"/></a><a href="/opportunities"><I18nText id="Marketplace"/></a></nav></header><section className="page-head"><div className="eyebrow"><I18nText id="Transaction Pipeline"/></div><h1><I18nText id="Investor Interests"/></h1><p><I18nText id="Every sensitive record remains organization-scoped."/></p></section><section className="panel"><div className="eyebrow"><I18nText id="INVESTOR"/></div><div className="table">{(items??[]).map((i:any)=><a className="row" href={`/workspace/interests/${i.id}`} key={`i-${i.id}`}><strong>{i.opportunities?.assets?.title||<I18nText id="Opportunity"/>}</strong><span>{[i.opportunities?.assets?.city,i.opportunities?.assets?.country_code].filter(Boolean).join(', ')||'—'}</span><span>{i.status}</span><span><I18nText id="Open workspace →"/></span></a>)}{!(items?.length)&&<div className="empty-state"><strong><I18nText id="No registered interests"/></strong><span><I18nText id="Open the marketplace to review available opportunities."/></span><a className="button secondary" href="/opportunities"><I18nText id="Marketplace"/></a></div>}</div></section><section className="panel" style={{marginTop:24}}><div className="eyebrow"><I18nText id="SELLER"/></div><div className="table">{(sellerItems??[]).map((i:any)=><a className="row" href={`/workspace/interests/${i.id}`} key={`s-${i.id}`}><strong>{i.opportunities?.assets?.title||<I18nText id="Opportunity"/>}</strong><span><I18nText id="Organizations"/>: {String(i.investor_organization_id).slice(0,8)}…</span><span>{i.status}</span><span><I18nText id="Review public opportunity information."/></span></a>)}{!(sellerItems?.length)&&<div className="empty-state"><strong><I18nText id="No registered interests"/></strong><span><I18nText id="Published opportunities"/></span></div>}</div></section></main>}
