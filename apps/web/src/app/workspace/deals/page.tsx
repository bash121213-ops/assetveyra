import { redirect } from 'next/navigation';
import { createClient } from '@/lib/supabase/server';
import { I18nText } from '@/components/LocaleShell';
export default async function DealsPage(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/login');const {data:members}=await s.from('organization_members').select('organization_id').eq('user_id',user.id);const ids=(members??[]).map((m:any)=>m.organization_id);let deals:any[]=[];if(ids.length){const {data}=await s.from('deals').select('id,status,target_close_date,created_at,updated_at,opportunities(slug,assets(title,currency,asking_price)),buyer_organization_id,seller_organization_id').or(`organization_id.in.(${ids.join(',')}),buyer_organization_id.in.(${ids.join(',')}),seller_organization_id.in.(${ids.join(',')})`).order('updated_at',{ascending:false});deals=data??[];}return <main className="app-shell"><header className="app-header"><a className="brand" href="/">ASSETVEYRA</a><nav><a href="/workspace"><I18nText id="Workspace"/></a><a href="/opportunities"><I18nText id="Marketplace"/></a></nav></header><section className="page-head"><div className="eyebrow"><I18nText id="DEALS"/></div><h1><I18nText id="Deal / Closing"/></h1><p><I18nText id="Follow the transaction lifecycle from initiation to closing."/></p></section><section className="panel"><div className="table">{deals.map((d:any)=><a className="row" key={d.id} href={`/workspace/deals/${d.id}`}><strong>{d.opportunities?.assets?.title||<I18nText id="Deal / Closing"/>}</strong><span>{d.status}</span><span>{d.target_close_date||<I18nText id="Location pending"/>}</span><span>{d.opportunities?.assets?.asking_price?`${d.opportunities.assets.currency||''} ${Number(d.opportunities.assets.asking_price).toLocaleString()}`:'—'}</span><span><I18nText id="Open workspace →"/></span></a>)}{!deals.length&&<div className="empty-state"><strong><I18nText id="No live deals."/></strong><span><I18nText id="Accepted offers and transaction records will appear here."/></span></div>}</div></section></main>}
