'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import { createClient } from '@/lib/supabase/client';
import { I18nText, useLocale } from '@/components/LocaleShell';
export default function SignupPage(){const router=useRouter();const locale=useLocale();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [accepted,setAccepted]=useState(false);const [busy,setBusy]=useState(false);const [message,setMessage]=useState('');const [error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setError('');setMessage('');if(!accepted){setError('Please acknowledge the Terms and Privacy Policy before creating an account.');return;}setBusy(true);const result=await createClient().auth.signUp({email,password,options:{data:{locale},emailRedirectTo:`${window.location.origin}/auth/callback?next=/dashboard`}});if(result.error)setError(result.error.message);else if(!result.data.session)setMessage('Account created. Check your email to verify the account, then sign in.');else router.push('/dashboard');setBusy(false);}return <main className="auth-page"><section className="auth-card"><a className="brand" href="/">ASSETVEYRA</a><div className="eyebrow"><I18nText id="SECURE ACCESS"/></div><h1><I18nText id="Create account"/></h1><p><I18nText id="Create an account to enter the AssetVeyra transaction network."/></p><form onSubmit={submit}><label><I18nText id="Email"/><input required type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label><I18nText id="Password"/><input required minLength={8} type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)}/></label><label style={{display:'flex',gap:8,alignItems:'flex-start'}}><input type="checkbox" checked={accepted} onChange={e=>setAccepted(e.target.checked)} required/><span><I18nText id="Terms/Privacy acknowledgement"/> — <a href="/terms"><I18nText id="Terms"/></a> / <a href="/privacy"><I18nText id="Privacy"/></a></span></label>{error&&<div className="form-error" role="alert"><I18nText id={error==='Please acknowledge the Terms and Privacy Policy before creating an account.'?error:'Something went wrong.'}/></div>}{message&&<div className="form-success" role="status"><I18nText id="Account created. Check your email to verify the account, then sign in."/></div>}<button className="button primary" disabled={busy}>{busy?<I18nText id="Processing…"/>:<I18nText id="Create account"/>}</button></form><a className="text-button" href="/login"><I18nText id="Sign in"/></a></section></main>;
}
