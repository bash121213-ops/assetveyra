'use client';
import { FormEvent, useState } from 'react';
import { I18nText } from '@/lib/i18n-home';
export default function ContactPage(){
 const [status,setStatus]=useState<'idle'|'sending'|'success'|'error'>('idle'); const [error,setError]=useState('');
 async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setStatus('sending');setError('');const form=new FormData(event.currentTarget);try{const response=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(form.entries()))});const data=await response.json();if(!response.ok)throw new Error(data.error||'Unable to send the request.');setStatus('success');event.currentTarget.reset();}catch(err){setStatus('error');setError(err instanceof Error?err.message:'Unable to send the request.');}}
 return <main className="app-shell"><header className="app-header"><a className="brand" href="/">ASSETVEYRA</a><nav><a href="/#opportunities"><I18nText id="Opportunities"/></a><a href="/#about"><I18nText id="About"/></a><a href="/login"><I18nText id="Sign in"/></a></nav></header><section className="form-page"><div className="eyebrow">ASSETVEYRA CONTACT</div><h1><I18nText id="Tell us what you are looking for."/></h1><p>For investment opportunities, asset submissions and business enquiries. We review the request before sharing controlled information.</p><form onSubmit={submit} className="form-grid"><label>Name<input name="name" required minLength={2} maxLength={120} autoComplete="name"/></label><label>Email<input name="email" type="email" required maxLength={320} autoComplete="email"/></label><label>Phone <span>(optional)</span><input name="phone" maxLength={40} autoComplete="tel"/></label><label>What can we help with?<select name="interest" defaultValue="investment"><option value="investment">Investment opportunity</option><option value="asset">Submit an asset</option><option value="partnership">Partnership</option><option value="general">General enquiry</option></select></label><label className="full">Message<textarea name="message" rows={7} required minLength={10} maxLength={4000} placeholder="Briefly tell us what you are looking for."/></label><div aria-hidden="true" style={{position:'absolute',left:'-10000px'}}><input name="website" tabIndex={-1} autoComplete="off"/></div><div className="full"><button className="button primary" type="submit" disabled={status==='sending'}>{status==='sending'?<I18nText id="Sending…"/>:<I18nText id="Send request"/>}</button>{status==='success'&&<p role="status"><I18nText id="Your request has been received."/></p>}{status==='error'&&<p role="alert">{error}</p>}</div></form></section></main>;
}
