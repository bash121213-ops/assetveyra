import { notFound,redirect } from 'next/navigation';
import { createClient } from '@/lib/supabase/server';
import { I18nText } from '@/components/LocaleShell';
const sectorKeys:Record<string,string>={land:'Land',residential:'Residential',commercial:'Commercial',hotel:'Hotel',hospitality:'Hospitality',industrial:'Industrial',mixed_use:'Mixed use',development_project:'Development project',infrastructure:'Infrastructure',renewable_energy:'Renewable energy',other:'Other'};
async function registerInterest(formData:FormData){'use server';const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/login');const slug=String(formData.get('slug')||'');const {data:opportunity}=await s.from('opportunities').select('id').eq('slug',slug).eq('status','published').maybeSingle();if(!opportunity)redirect('/opportunities');const {data:membership}=await s.from('organization_members').select('organization_id,organizations(type)').eq('user_id',user.id).limit(20);const investor=(membership??[]).find((m:any)=>m.organizations?.type==='investor');if(!investor)redirect('/onboarding?next=/opportunities/'+encodeURIComponent(slug));const {data:existing}=await s.from('investor_interests').select('id').eq('investor_organization_id',investor.organization_id).eq('opportunity_id',opportunity.id).maybeSingle();if(!existing){const {error}=await s.from('investor_interests').insert({investor_organization_id:investor.organization_id,opportunity_id:opportunity.id,status:'interest',created_by:user.id});if(error)redirect('/dashboard?error=interest');}redirect('/dashboard?interest=registered');}
export default async function OpportunityPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=await createClient();const {data:{user}}=await s.auth.getUser();const {data:o}=await s.from('public_opportunities').select('id,slug,status,investment_thesis,structure,minimum_ticket,target_return,asset_id').eq('slug',slug).maybeSingle();if(!o)notFound();const {data:asset}=await s.from('public_assets').select('id,title,asset_type,country_code,region,city,area_sqm,currency,asking_price,public_summary').eq('id',o.asset_id).maybeSingle();if(!asset||!asset.asking_price||Number(asset.asking_price)<100000)redirect('/opportunities');if(!user)redirect('/login');return <main className="app-shell"><header className="app-header"><a className="brand" href="/">ASSETVEYRA</a><nav><a href="/opportunities"><I18nText id="Opportunities"/></a><a href="/dashboard"><I18nText id="Dashboard"/></a><form action="/logout" method="post"><button className="text-button"><I18nText id="Sign out"/></button></form></nav></header><article className="detail"><div className="eyebrow"><I18nText id="PUBLISHED OPPORTUNITY · CONTROLLED ACCESS"/></div><h1>{asset.title}</h1><div className="detail-grid"><section><p className="lead">{asset.public_summary||o.investment_thesis}</p><div className="facts"><div><span><I18nText id="Asset type"/></span><strong>{sectorKeys[asset.asset_type]?<I18nText id={sectorKeys[asset.asset_type]}/>:asset.asset_type}</strong></div><div><span><I18nText id="Location"/></span><strong>{[asset.city,asset.region,asset.country_code].filter(Boolean).join(', ')}</strong></div><div><span><I18nText id="Area m²"/></span><strong>{asset.area_sqm?`${Number(asset.area_sqm).toLocaleString()} m²`:'—'}</strong></div><div><span><I18nText id="Asking price"/></span><strong>{`${asset.currency||'USD'} ${Number(asset.asking_price).toLocaleString()}`}</strong></div><div><span><I18nText id="Minimum ticket"/></span><strong>{o.minimum_ticket?`${asset.currency||'USD'} ${Number(o.minimum_ticket).toLocaleString()}`:'—'}</strong></div><div><span><I18nText id="Target return"/></span><strong>{o.target_return?`${o.target_return}%`:'—'}</strong></div></div><div className="panel" style={{margin:'55px 0 0',padding:'35px 0'}}><div className="eyebrow"><I18nText id="TRANSACTION PATH"/></div><div className="lifecycle"><div className="lifecycle-step"><span>01</span><strong><I18nText id="Interest"/></strong></div><div className="lifecycle-step"><span>02</span><strong><I18nText id="Qualify"/></strong></div><div className="lifecycle-step"><span>03</span><strong><I18nText id="NDA"/></strong></div><div className="lifecycle-step"><span>04</span><strong><I18nText id="Data Room"/></strong></div><div className="lifecycle-step"><span>05</span><strong><I18nText id="Due Diligence"/></strong></div><div className="lifecycle-step"><span>06</span><strong><I18nText id="Offer"/></strong></div></div></div></section><aside className="deal-gate"><div className="eyebrow"><I18nText id="INVESTOR ACCESS"/></div><h2><I18nText id="Register qualified interest"/></h2><p><I18nText id="You are viewing the controlled opportunity information available to registered users. Additional ownership, legal, financial or technical material may require qualification, confidentiality terms or Data Room access depending on the opportunity."/></p><form action={registerInterest}><input type="hidden" name="slug" value={slug}/><button className="button primary" type="submit"><I18nText id="Register interest"/></button></form></aside></div></article></main>}
